import React from 'react'

const styles = {
  root: {
    flexGrow: 1,
    height: 440,
    zIndex: 1,
    overflow: 'hidden',
    position: 'relative',
    display: 'flex',
  }
}

const HomeView = () => {
  return (
    <div style={styles.root}>
    </div>
  )
}

export default HomeView
